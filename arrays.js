// JavaScript arrays and methods
let names = ['Lance', 'John', 'Alice'];
let numbers = [2, 4, 6, 8, 10];
let mixedValues = ['Lance', 2, true];

// How to determine array length
let arrLength = names.length;
console.log(arrLength);

// How to read an array
console.log(names[1]);

// How to update array
names[0] = 'Alex';
console.log(names);

// Deleting arrays
names.pop(); // remove the last value
names.shift(); // remove the first values
names.slice(start, end);
